<header>
  <nav class="nav-links">


    <% if logged_in %>
      <ul>
        <li><%= link_to("#{current_user.name.titlecase}", user_path(current_user)) %></li>
        <li><%= link_to("Analysis", user_analysis_show_path(current_user)) %></li>
        <li style="float: right"><%= link_to "Log out", logout_path, method: :delete, class: "active" %></li>
      </ul>
    <% end %>


  </nav>
</header>